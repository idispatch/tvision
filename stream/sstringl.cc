#ifndef NO_STREAM

#define Uses_TStringList

#include <tv.h>

TStreamableClass RStringList( TStringList::name,
                              TStringList::build,
                              __DELTA(TStringList)
                            );

#endif

