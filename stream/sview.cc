#ifndef NO_STREAM

#define Uses_TView

#include <tv.h>

TStreamableClass RView( TView::name,
                        TView::build,
                        __DELTA(TView)
                      );

#endif

