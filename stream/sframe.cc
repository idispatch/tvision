#ifndef NO_STREAM

#define Uses_TFrame

#include <tv.h>

TStreamableClass RFrame( TFrame::name,
                         TFrame::build,
                         __DELTA(TFrame)
                       );

#endif
