#ifndef NO_STREAM

#define Uses_TMenuBox

#include <tv.h>

TStreamableClass RMenuBox( TMenuBox::name,
                           TMenuBox::build,
                           __DELTA(TMenuBox)
                         );

#endif

