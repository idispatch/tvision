#ifndef NO_STREAM

#define Uses_TDeskTop

#include <tv.h>

TStreamableClass RDeskTop( TDeskTop::name,
                           TDeskTop::build,
                           __DELTA(TDeskTop)
                         );

#endif

