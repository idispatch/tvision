#ifndef NO_STREAM

#define Uses_TFileList

#include <tv.h>

TStreamableClass RFileList( TFileList::name,
                            TFileList::build,
                            __DELTA(TFileList)
                          );

#endif

